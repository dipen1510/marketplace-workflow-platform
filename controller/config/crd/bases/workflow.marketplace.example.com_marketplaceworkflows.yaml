---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: marketplaceworkflows.workflow.marketplace.example.com
spec:
  group: workflow.marketplace.example.com
  names:
    kind: MarketplaceWorkflow
    listKind: MarketplaceWorkflowList
    plural: marketplaceworkflows
    singular: marketplaceworkflow
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MarketplaceWorkflow is the Schema for the marketplaceworkflows
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of MarketplaceWorkflow
            properties:
              concurrencyPolicy:
                default: Forbid
                enum:
                - Allow
                - Forbid
                - Replace
                type: string
              failedRunsHistoryLimit:
                format: int32
                minimum: 0
                type: integer
              parameters:
                description: Parameters passed to the Argo workflow.
                items:
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              schedules:
                description: Schedules defines when this workflow runs.
                items:
                  type: string
                minItems: 1
                type: array
              startingDeadlineSeconds:
                format: int64
                minimum: 0
                type: integer
              successfulRunsHistoryLimit:
                format: int32
                minimum: 0
                type: integer
              suspend:
                description: Suspend prevents new scheduled executions.
                type: boolean
              timezone:
                description: Timezone used when evaluating the schedules.
                type: string
              workflowTemplateRef:
                description: Argo WorkflowTemplate that will actually execute.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
            required:
            - schedules
            - workflowTemplateRef
            type: object
          status:
            description: status defines the observed state of MarketplaceWorkflow
            properties:
              activeRuns:
                description: Current number of active Argo runs.
                format: int32
                type: integer
              conditions:
                description: Standard Kubernetes conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              cronWorkflowName:
                description: Name of the Argo CronWorkflow created by this controller.
                type: string
              failedRuns:
                description: Number of failed executions reported by Argo.
                format: int64
                type: integer
              lastScheduledTime:
                description: Last time Argo scheduled this workflow.
                format: date-time
                type: string
              observedGeneration:
                description: Generation observed by the controller.
                format: int64
                type: integer
              phase:
                description: Current high-level state.
                type: string
              succeededRuns:
                description: Number of successful executions reported by Argo.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
